"""
Пакет ``gui`` — графический интерфейс на Tkinter.

**Назначение:** окно настроек, лог, запуск :func:`application.runner.run` в фоне, открытие каталога артефактов.

**Разрешённые зависимости:** ``application``, ``config``, ``shared``, ``core`` (подсказки WG).

**Запрещено:** ``cli``, ``server``, ``commands`` (действия только через ``application``).

**Публичный API:** :func:`gui.gui_tk.main` и вспомогательные модули ``gui.gui_*``.
"""
