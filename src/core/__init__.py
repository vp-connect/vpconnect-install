"""
Пакет ``core`` — чистые правила и разбор данных (без SSH, GUI, HTTP-клиентов).

**Назначение:** нормализация WG-сети, разбор URL GitHub и др. логика без побочных эффектов.

**Разрешённые зависимости:** ``shared`` (константы), стандартная библиотека.

**Запрещено:** ``application``, ``server``, ``commands``, ``config``, ``cli``, ``gui``.

**Публичный API:** :mod:`core.wg_client_network`, :mod:`core.github_repo`.
"""
